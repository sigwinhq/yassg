services:
    imgproxy:
        image: darthsim/imgproxy:v3
        environment:
            - IMGPROXY_LOCAL_FILESYSTEM_ROOT=/app
        working_dir: /app
        ports:
            - 8080:8080
        volumes:
            - .:/app:ro
        tmpfs:
            - /tmp
